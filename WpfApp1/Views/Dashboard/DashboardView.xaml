<UserControl x:Class="WpfApp1.Views.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="24" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontSize="28"
                       FontWeight="SemiBold"
                       Text="{Binding Title}" />
            <TextBlock Margin="0,8,0,0"
                       Foreground="#666666"
                       Text="{Binding StatusMessage}" />
        </StackPanel>

        <UniformGrid Grid.Row="2" Columns="2" Rows="2">
            <Border Margin="0,0,12,12" Padding="20" Background="#F2F4F8" CornerRadius="12">
                <StackPanel>
                    <TextBlock FontSize="14" Foreground="#666666" Text="Mode" />
                    <TextBlock Margin="0,8,0,0" FontSize="24" FontWeight="SemiBold" Text="{Binding CurrentMode}" />
                </StackPanel>
            </Border>
            <Border Margin="0,0,0,12" Padding="20" Background="#F2F4F8" CornerRadius="12">
                <StackPanel>
                    <TextBlock FontSize="14" Foreground="#666666" Text="Connection" />
                    <TextBlock Margin="0,8,0,0" FontSize="18" FontWeight="SemiBold" Text="{Binding ConnectionSummary}" />
                </StackPanel>
            </Border>
            <Border Margin="0,0,12,0" Padding="20" Background="#F2F4F8" CornerRadius="12">
                <StackPanel>
                    <TextBlock FontSize="14" Foreground="#666666" Text="Production" />
                    <TextBlock Margin="0,8,0,0" FontSize="18" Text="{Binding TotalCount, StringFormat=Total: {0}}" />
                    <TextBlock Margin="0,4,0,0" FontSize="18" Text="{Binding OkCount, StringFormat=OK: {0}}" />
                    <TextBlock Margin="0,4,0,0" FontSize="18" Text="{Binding NgCount, StringFormat=NG: {0}}" />
                </StackPanel>
            </Border>
            <Border Padding="20" Background="#F2F4F8" CornerRadius="12">
                <StackPanel>
                    <TextBlock FontSize="14" Foreground="#666666" Text="Latest Inspection" />
                    <TextBlock Margin="0,8,0,0" FontSize="18" FontWeight="SemiBold" Text="{Binding LatestInspectionResult}" />
                </StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>
